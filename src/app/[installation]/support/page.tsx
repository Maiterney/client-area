import { SupportContainer } from "@/templates/Suport";
import { api } from "@/utils";
import { cookies } from "next/dist/client/components/headers";

export default async function SupportPage() {

    return <SupportContainer/>
}